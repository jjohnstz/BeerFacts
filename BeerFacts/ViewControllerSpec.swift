import Foundation

import Quick
import Nimble

class ViewControllerSpec: QuickSpec {
    override func spec() {
        describe("ViewController") {
            
            it("should be setup") {
                expect(true).to(be(true))
            }

        }
    }
}
